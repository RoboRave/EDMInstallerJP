version = System.env.TRAVIS_BUILD_NUMBER
group 'com.edminstallerjp'
archivesBaseName='ModInstaller'
apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
	 compile files('lib/AbsoluteLayout.jar')
	 compile files('lib/gson-2.4.jar')
}
